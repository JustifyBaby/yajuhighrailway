import { root } from "../global"

const ToPlan = () => {
  return (
    <div>
      <h2>
        <a href={`${root}/plan`}>中射姫線計画について</a>
      </h2>
    </div >
  )
}

export default ToPlan